name: env feature
on: 
    push:
jobs:
    job_1:
        if: contains(github.ref,'main' )
        runs-on: ubuntu-latest
        environment: production
        steps:
            - name: deploy
              run: echo "${{vars.environment_txt}}"
            - run: echo "${{github.ref_name}}"
            - run: echo "${{github.ref}}"
    job2:
        if: contains(github.ref,'dev' ) 
        runs-on: ubuntu-latest
        environment: staging
        steps:
            - name: deploy
              run: echo "${{vars.environment_txt}}"
           